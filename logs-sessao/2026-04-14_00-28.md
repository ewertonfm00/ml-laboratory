# Log de Sessão — 2026-04-14

## O que foi feito

### n8n
- Importados 16 workflows via API (com remoção de campos read-only e encoding UTF-8)
- Corrigidos 4 bugs no workflow `ML-CAPTURA-whatsapp-pipeline`:
  1. **Lookup Setor**: query usava `remote_jid` (JID do cliente) em vez de `session_id` (nome da instância)
  2. **Enriquecer com Setor**: referenciava node por ID (`normalize-payload`) em vez de nome (`Normalizar Payload`)
  3. **Filtro Áudio/Texto**: `mode: runOnceForEachItem` incompatível com a versão do n8n — migrado para `runOnceForAllItems`
  4. **Inserir mensagens_raw**: query misturava `{{ }}` n8n com `$1/$2` PostgreSQL — convertido tudo para expressões n8n
  5. **Normalizar Payload**: filtrava `fromMe: true` impedindo captura de mensagens dos agentes — removido filtro

### Banco de dados
- Migrations 001→010 executadas com sucesso no Postgres Railway
- Projeto "Omega Laser Locações" criado em `_plataforma.projetos`
- Instância `omega-laser-locacoes` registrada em `_plataforma.instancias_evolution`
- Número `551632363666` cadastrado em `_plataforma.numeros_projeto`
- Agentes Tabata, Rodrigo, Larissa, Ewerton cadastrados em `_plataforma.agentes_humanos`

### Webhooks
- Evolution API `omega-laser-locacoes` → webhook configurado e depois **desativado** (uso do Redrive)
- Redrive webhook configurado apontando para `n8n ML-CAPTURA`
- Pipeline testado com sucesso — mensagem real gravada no banco

## Decisões tomadas

- Usar Redrive como fonte de webhook (não Evolution API diretamente) para evitar duplicatas
- Capturar `fromMe: true` (mensagens dos agentes) além das mensagens dos clientes
- Identificação de agente adiada — Redrive não inclui agente no payload do webhook

## Problema identificado (usuário reportou)

O nó "Lookup Setor" ainda pode falhar se:
- `instance_name` no banco não bate exatamente com `session_id` do payload
- A tabela `ml_captura.setores` não existe (referência errada no workflow)

## Próximos passos

1. @dev varredura de inconsistências no projeto
2. Verificar Redrive: há forma de identificar agente no webhook?
3. Seed usuário MASTER
4. Limpar workflows duplicados no n8n
