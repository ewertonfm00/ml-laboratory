# Log de Sessão — 2026-04-15

**Duração:** tarde/noite
**Foco:** Pipeline de captura Redrive → n8n + correções no ML-CAPTURA

---

## O que foi feito

### Chatflow Redrive
- Chatflow anterior (`qdoHti2f7Pl2I06kFCQO`) foi excluído pelo usuário
- Novo chatflow criado do zero com bloco de integração (POST) configurado
- URL configurada: `https://n8n-production-47d0.up.railway.app/webhook/ml/webhook/whatsapp`
- Body atual: `{"source": "redrive", "timestamp": "{createdate}"}` — mínimo, pendente enriquecimento
- `{createdate}` chegou como texto literal — variável não resolvida, sintaxe a confirmar

### Correções no workflow ML-CAPTURA (n8n)
**Problema 1:** Nó "Groq Whisper — Transcrição" com triângulo amarelo impedindo publicação
- Causa raiz 1: `authentication: "genericCredentialType"` sem credential vinculada → corrigido para `authentication: "none"`
- Causa raiz 2: header Authorization com sintaxe incorreta `=Bearer {{ $env.ML_GROQ_API_KEY }}` → corrigido para `={{ 'Bearer ' + $vars.ML_GROQ_API_KEY }}`
- Causa raiz 3: body params usando `$env.ML_GROQ_WHISPER_MODEL/LANGUAGE` → corrigido para `$vars.*`
- Método: atualização via API do n8n (PUT /api/v1/workflows/eM0qnKGXShlOuCsV)
- Workflow reativado via POST /activate — confirmado `active: true`

### Testes de webhook
- Teste manual via curl: `200 OK` + `{"message":"Workflow was started"}`
- Execução 4214 (teste manual): payload chegou corretamente
- Execução 4168 (Redrive real): body chegou com apenas `source` e `timestamp` literal

### Investigação da API Redrive
- Endpoints de autenticação (`/v1/auth/login`, `/auth/login`) retornam 404
- Swagger disponível apenas como HTML, sem spec JSON acessível
- `/v1/crm/getbyphone` não testado — sem token de autenticação disponível
- Conclusão: enriquecimento de dados deve ser feito no próprio chatflow (tree-builder), não via chamada extra à API

### Levantamento Evolution API
- 3 instâncias existentes: `ml-empresa-teste-final`, `ml-empresa-teste-qa`, `omega-laser-locacoes`
- Todas com status `connecting` e sem número conectado
- Decisão confirmada: número 1632363666 fica exclusivo no Redrive — não conectar na Evolution API própria

### Estratégia de captura definida
- Opção escolhida: capturar **toda mensagem recebida** (lead + atendente) para análise completa de padrões
- Pipeline: chatflow Redrive → body enriquecido → n8n → Normalizar Payload → banco

---

## Decisões tomadas

| Decisão | Motivo |
|---------|--------|
| Capturar toda mensagem (não só atendente) | Objeções e contexto do lead são necessários para análise |
| Não usar webhook global Redrive | Confirmado que não dispara para mensagens recebidas |
| Não usar Evolution API para 1632363666 | Conflito com Redrive — mesma instância interna |
| Enriquecer dados no chatflow, não via API Redrive | API sem autenticação acessível; tree-builder é mais simples |
| Usar `$vars` em vez de `$env` no Groq Whisper | Chave cadastrada em n8n Variables, não Railway env |

---

## Pendências abertas ao final da sessão

1. Adicionar variáveis reais no body do bloco de integração Redrive
2. Publicar chatflow e testar com mensagem real
3. Adaptar nó "Normalizar Payload" para formato Redrive confirmado
4. Validar inserção em `ml_captura.mensagens_raw`
5. Validar telas Appsmith visualmente

---

## Comandos relevantes executados

```bash
# Testar webhook
curl -X POST https://n8n-production-47d0.up.railway.app/webhook/ml/webhook/whatsapp \
  -H "Content-Type: application/json" \
  -d '{"source":"teste-manual",...}'

# Atualizar workflow via API n8n
curl -X PUT https://n8n-production-47d0.up.railway.app/api/v1/workflows/eM0qnKGXShlOuCsV \
  -H "X-N8N-API-KEY: ..." -d @workflow.json

# Ativar workflow
curl -X POST https://n8n-production-47d0.up.railway.app/api/v1/workflows/eM0qnKGXShlOuCsV/activate
```
