# Log de Sessão — 2026-04-23

## O que foi feito

### Execuções por agentes (disparadas em paralelo)
- **@devops**: Redeploy portal-ml via `railway redeploy --service portal-ml` — container reiniciou. Depois `railway up` para buildar código novo.
- **@dev**: 3 correções arquiteturais — `agent-delivery-manager.md` criado, gate de segmento no `onboarding-orchestrator`, output intrínseco no `profile-segment-matcher`. Commits: `8b6232c`.
- **@dev**: Fix nó "Normalizar Payload" n8n — `push_name` adicionado, fallbacks incorretos removidos. Commit: `8877ecf`.

### Sincronização n8n via API
- Workflow `eM0qnKGXShlOuCsV` sincronizado 4 vezes via `PUT /api/v1/workflows/:id`
- **Issue descoberta**: arquivo JSON local tem encoding corrompido (curly quotes U+201C/U+201D usadas como delimitadores JSON, mojibake em-dash). Sanitização Python necessária antes de cada PUT.

### Bugs identificados e corrigidos
1. **Normalizar Payload — body wrapper** (commit `c13a802`): `const body = $input.first().json` quebrou o pipeline — payload da Evolution API está em `$json.body`. Restaurado para `$json.body || $json`.
2. **Upsert sessoes_conversa — multi-statement** (commit `28fc077`): dois SQL separados por `;` — driver `pg` só executava o primeiro. Substituído por `INSERT ON CONFLICT DO UPDATE`.
3. **Upsert sessoes_conversa — coluna inexistente** (commit `285b767`): `mr.agente_humano_id` não existe em `mensagens_raw`. Coluna removida da query.

### Validação do pipeline
- `mensagens_raw`: confirmado com mensagens reais (vários `remote_jid`, conteúdo real)
- `sessoes_conversa`: último fix aplicado mas não confirmado (usuário encerrou sessão antes do teste final)

### Commits desta sessão (além dos pendentes anteriores)
- `8b6232c` — correções arquiteturais squads
- `8877ecf` — fix Normalizar Payload (push_name + paths)
- `c13a802` — restaurar body wrapper ($json.body || $json)
- `28fc077` — upsert ON CONFLICT DO UPDATE
- `285b767` — remover mr.agente_humano_id
- `9a3fa76` — squad names PT + titles
- `25a084f` — .claude files + knowledge-base

## Erros resolvidos

| Erro | Causa | Fix |
|------|-------|-----|
| Mensagens não capturadas após fix | `$json.body` quebrado | Restaurar `|| $json` fallback |
| sessoes_conversa não atualizava | 2 SQL statements num node | ON CONFLICT DO UPDATE |
| `column mr.agente_humano_id does not exist` | mensagens_raw não tem essa coluna | Remover da query |

## Decisões tomadas

- **Sanitização de encoding**: toda vez que o workflow JSON for atualizado via API, usar script Python com substituição explícita de curly quotes e surrogates
- **`railway redeploy` vs `railway up`**: `redeploy` reinicia container existente (sem novo código); `railway up` faz build do código-fonte atual
- **`/api/diagnostico`** foi removida intencionalmente (commit `f506289`) — 404 é comportamento correto

## Pendências para próxima sessão

- Confirmar `sessoes_conversa` atualizado (enviar mensagem e checar `total_mensagens`)
- Seed MASTER (e-mail + senha do usuário)
- Push para origin/main via @devops
