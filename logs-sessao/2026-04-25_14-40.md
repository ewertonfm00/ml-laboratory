# Log de Sessão — 2026-04-25

## O que foi feito

### 1. Sincronização JSON ML-CAPTURA (commit e6cf96d)
- JSON local `infra/n8n/workflows/ML-CAPTURA-whatsapp-pipeline.json` estava desatualizado
- Buscou JSON atual da API do n8n (produção) e salvou localmente
- 16 nós, versão a6090f84

### 2. Debug Pipeline criado e corrigido (commits 6ccc535 + anterior)
- Criado `infra/n8n/workflows/ML-CAPTURA-debug-pipeline.json`
- Importado no n8n via API → id `ndNJ8wXr1xoN8ZAs`
- **Erro identificado:** arquitetura paralela (3 postgres em paralelo → Code node) disparava o Code assim que o primeiro paralelo terminava — `Stats Sessoes` e `Stats Agentes` ainda não tinham executado
- **Correção:** mudou para sequencial: Webhook → Stats Mensagens → Stats Sessoes → Stats Agentes → Gerar Diagnostico → Respond
- Atualizado via PUT na API do n8n (campo `meta` é read-only, não vai no body)
- Testado: retorna snapshot com 1.229 mensagens, 90 sessões, alertas WARN (88 sessões sem agente)

### 3. Migration 020 — webhooks_config (commit 4f78df3)
- Criado `database/migrations/020_captura_webhooks_config.sql`
- Criado `database/migrations/rollbacks/020_captura_webhooks_config_rollback.sql`
- Tabela `ml_captura.webhooks_config` criada em produção
- FK para `_plataforma.instancias_evolution`, UNIQUE por instância, CHECK em status (ativo/inativo/erro), trigger updated_at
- Desbloqueou task `configure-webhook` do ml-captura-squad

### 4. settings.json (commit fee54d5)
- Path do script `squad-clickup-sync.py` corrigido de relativo para absoluto

## Erros resolvidos

| Erro | Causa | Solução |
|------|-------|---------|
| Debug pipeline "Error in workflow" | Code node disparava antes dos nós paralelos terminarem | Mudou de paralelo para sequencial |
| PUT n8n 400 Bad Request | API rejeita campos internos (id, createdAt, etc.) e `meta` (read-only) | Envia apenas name, nodes, connections, settings, staticData, pinData |

## Decisões técnicas

- **Sequencial > Paralelo no n8n** para nós que precisam esperar uns pelos outros sem Merge node explícito
- **webhooks_config com UNIQUE em instancia_id** — cada instância Evolution API tem no máximo uma config ativa

## Estado do banco ao encerrar

- 20 migrations aplicadas
- 1.229 mensagens em `ml_captura.mensagens_raw`
- 90 sessões ativas (88 sem agente_humano_id)
- 1 agente vinculado: Kátia - Cosmobeauty com 2 sessões

## Próximo passo

Criar workflow n8n `configure-webhook` usando a tabela `ml_captura.webhooks_config` recém-criada.
