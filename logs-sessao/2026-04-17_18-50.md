# Log de Sessão — 2026-04-17

## Resumo

Deploy completo do portal Next.js no Railway.

---

## O que foi feito

### 1. Build local do portal Next.js
- Verificado que `portal-next/` já existia com código commitado
- Executado `npx next build` (npm não tinha `next` no PATH)
- Build passou: 9 rotas compiladas sem erros

### 2. Push para GitHub
- Adicionado `portal-next/package-lock.json` (gerado pelo npm install)
- Commit: `chore: adiciona package-lock.json portal-next após npm install`
- Push para `origin main` — sucesso

### 3. Deploy no Railway via GraphQL API
- Railway CLI não conseguia criar serviço (`railway add` retornava "Unauthorized")
- Solução: usar Railway GraphQL API diretamente com `accessToken` do config.json
- Serviço `portal-ml` criado (ID: `616c8604-6a56-441f-a5e4-90d8033adf1d`)
- `rootDirectory` configurado como `portal-next`
- `buildCommand: npm run build` / `startCommand: npm start`
- Variáveis de ambiente configuradas via `variableCollectionUpsert`:
  - `DATABASE_URL`, `N8N_SETUP_URL`, `EVOLUTION_API_URL`, `EVOLUTION_API_KEY`, `NODE_ENV`
- Deploy disparado via `serviceInstanceDeployV2`
- Domínio gerado: `portal-ml-production.up.railway.app`
- Deploy finalizado com status `SUCCESS`
- Portal respondendo HTTP 200

---

## Decisões tomadas

- Railway CLI v4 tem bug de autorização no comando `add` mesmo com usuário autenticado → contornar via API
- Token de sessão (`accessToken`) encontrado em `%APPDATA%/railway/config.json`
- Token RAILWAY_TOKEN do `.env` estava vazio/expirado → não usar mais, usar accessToken do config

---

## Erros resolvidos

| Erro | Causa | Solução |
|------|-------|---------|
| `next não reconhecido` no build | next.js instalado localmente, não global | Usar `npx next build` |
| `railway add` Unauthorized | Bug no Railway CLI v4 | Usar Railway GraphQL API diretamente |
| Token `fee10a52-...` inválido | Token expirado na memória | Usar `accessToken` do `~/.railway/config.json` |

---

## URLs criadas nesta sessão

- **Portal ML (produção):** `https://portal-ml-production.up.railway.app`

---

## Próxima sessão

Conectar número WhatsApp via `https://portal-ml-production.up.railway.app/numeros/conectar`
