# Log de Sessão — 2026-04-26 19:08

## Objetivo da sessão
Iniciar testes pendentes das Stories 1.1 e 1.2 + investigar por que o portal não exibia dados.

---

## O que foi feito

### Diagnóstico do banco de dados
- Confirmado que migrations 015, 016 e 017 estão aplicadas no banco `railway`
- `numeros_projeto`: campos `tipo` e `agente_default_id` presentes
- `sessoes_conversa`: campo `agente_humano_id` presente
- `mensagens_raw`: campos `direction`, `respondent_type`, `respondent_name` presentes
- Número `5516988456918` configurado como `tipo=mono`, `agente_default_id` = Kátia (55c1950e)

### Diagnóstico das sessões
- 52 sessões no projeto Machine Learning, 1166+ mensagens capturadas
- `contato_nome` vazio em TODAS as sessões — bug ainda não resolvido
- `agente_humano_id` nulo na maioria das sessões (apenas 1 sessão de 24/04 com Kátia vinculada)
- Mensagens capturadas de grupo (`@g.us`) e contatos individuais

### Fix crítico: portal .env.local
- **Bug:** `portal-next/.env.local` apontava para banco `postgres` (que existe no Railway mas está VAZIO — 0 sessões, 0 mensagens)
- **Correto:** dados reais estão no banco `railway` (mesmo servidor, diferente database name)
- **Fix:** `DATABASE_URL=.../postgres` → `DATABASE_URL=.../railway`
- Portal reiniciado, dados aparecem corretamente agora
- `http://localhost:3001/p/machine-learning/conversas` exibe as 52+ sessões

### Estrutura do banco (descoberta)
- O Railway provisiona 2 bancos no mesmo servidor: `railway` (prod) e `postgres` (empty/default)
- Sempre usar `/railway` na connection string

---

## Erros / Problemas encontrados

| Problema | Status |
|----------|--------|
| Portal sem dados — apontava para banco errado | ✅ Resolvido |
| `contato_nome` vazio em todas as sessões | ⏳ Pendente (testes não executados) |
| `agente_humano_id` nulo na maioria das sessões | ⏳ Pendente |

---

## Decisões tomadas
- Portal local na porta 3001 (não implantado no Railway ainda)
- Testes das Stories 1.1 e 1.2 adiados para próxima sessão

---

## Agentes envolvidos
- Claude Code (diagnóstico inicial)
- @dev (Dex) — fix do portal .env.local

---

## Comandos relevantes executados
```bash
# Verificar banco correto
psql "...railway" -c "SELECT COUNT(*) FROM ml_captura.sessoes_conversa"
# → 52 sessões

# Banco errado (era o que o portal usava)
psql "...postgres" -c "SELECT COUNT(*) FROM ml_captura.sessoes_conversa"
# → 0 sessões

# Fix aplicado em portal-next/.env.local
DATABASE_URL=postgresql://postgres:***@mainline.proxy.rlwy.net:13932/railway
```
